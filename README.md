# Date-Romantico
Site estático em HTML, CSS e JavaScript.
Para GitHub Pages: envie `index.html`, `style.css` e `script.js` para a raiz do repositório e ative Settings > Pages > Deploy from a branch > main > / (root).
